<% @torrent_files.each do |f| %>
    <p><%= link_to f.name, download_url(:id => f.id) %>, size: <%= number_to_human_size(f.size, :prefix => :si, :precision => 1, :significant => false) %>, seeders: <%= f.torrent.seeders.count %>, leechers: <%= f.torrent.leechers.count %><p>
<% end %>
